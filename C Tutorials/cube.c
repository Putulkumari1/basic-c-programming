#include<stdio.h>

int main()  {           // area of square

       int num ,a;
       printf("enter a num: ");
       scanf("%d", &a);
        
       num=(a*a*a);
       printf("num is %d", num);
    
    return 0;
}