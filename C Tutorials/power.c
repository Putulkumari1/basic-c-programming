#include<stdio.h>
# include<math.h>
int main()  {           // power any no. use math.h operator and this program is power operator

       int b ,a;
       b=a=2;
       int c=b+a;
       int power=pow(b,a);
        
       
       printf("power is %d", power);
    
    return 0;
}