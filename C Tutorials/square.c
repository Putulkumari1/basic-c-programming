   #include<stdio.h>

int main()  {           // area of square

       int side;
       printf("enter side");
       scanf("%d", &side);
        
       
       printf("sum is %d", side*side);
    
    return 0;
}